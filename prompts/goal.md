# /goal

Canonical source: `docs/product/goal.md`.

When invoked, read the canonical goal and restate the task in terms of the product lifecycle:

> Discover -> Brief -> Score -> Model -> Validate -> Test -> Deploy -> Run -> Approve -> Observe -> Recover -> Measure -> Reuse -> Improve

Do not reinterpret the goal from memory. If the requested work conflicts with the canonical goal, surface the conflict before implementation.
